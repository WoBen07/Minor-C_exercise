#include <stdio.h>
#include <math.h>

int main(void)
{
        int F, H;
        H = 'H';
        F = '?';
        if (7 > 0 && 7 < 89) {
                printf("Wahr\n");
        } else {
                printf("Falsch\n");
        }
        if (F > H)
        {
                printf("Wahr\n");
        } else {
                printf("Falsch\n");
        }
        if ('m' == 109)
        {
                printf("Wahr\n");
        } else {
                printf("Falsch\n");
        }
        
        
        return 0;
}